---

---

<section class="footer-container">
    <p>&copy; <span id="current-year"></span> Miguel Páez. Todos los derechos reservados.</p>
</section>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const yearElement = document.getElementById('current-year');
        
        if (yearElement) {
            yearElement.textContent = new Date().getFullYear().toString();
        }
    });
</script>

<style>
    .footer-container {
        width: var(--width-container);
        max-width: 1200px;
        margin: 0 auto;
        padding: 5rem 1.3rem;
        text-align: center;
        border-top: 1px solid var(--color-primary);
    }
    .footer-container p {
        font-family: var(--font-text);
        color: var(--color-text);
        font-size: 1.2rem;
        opacity: 0.6;
    }
    @media (width < 1200px) {
        .footer-container {
            width: 90%;
            padding: 3rem 1.3rem;
        }
    }
</style>