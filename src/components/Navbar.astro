---
import ButtonColor from "./ButtonColor.astro";
---

<nav class="navbar" id="navbar">
  <div class="nav-content">
    <div class="logo-box">
      <a href="/" class="logo">MP88</a>
    </div>

    <button
      class="hamburger"
      id="hamburger"
      aria-expanded="false"
      aria-controls="menu-panel"
      aria-label="Menú de navegación"
    >
      <span class="bar"></span>
      <span class="bar"></span>
      <span class="bar"></span>
    </button>
    <div class="menu-panel" id="menu-panel">
      <ul class="menu-links">
        <li class="link">
          <a href="/about/" class="menu-link link-click link-hover">
            <div class="link-content">
              <div class="link-text">
                <span>About</span>
                <span class="duplicate">About</span>
              </div>
            </div>
          </a>
        </li>
        <li class="link">
          <a href="/blog/" class="menu-link link-click link-hover">
            <div class="link-content">
              <div class="link-text">
                <span>Blog</span>
                <span class="duplicate">Blog</span>
              </div>
            </div>
          </a>
        </li>
        <li class="link">
          <a href="/projects/" class="menu-link link-click link-hover">
            <div class="link-content">
              <div class="link-text">
                <span>Projects</span>
                <span class="duplicate">Projects</span>
              </div>
            </div>
          </a>
        </li>
      </ul>
      <ButtonColor />
    </div>
  </div>
</nav>

<script>
  // Scroll Logic (comentada como en tu original)
  /*
  let ticking = false;
  window.addEventListener("scroll", () => {
    if (!ticking) {
      window.requestAnimationFrame(() => {
        const navbar = document.getElementById("navbar");
        if (navbar) {
          navbar.classList.toggle("scrolled", window.scrollY > 2);
        }
        ticking = false;
      });
      ticking = true;
    }
  }); 
   */

  const hamburger = document.getElementById("hamburger");
  const navMenu = document.getElementById("menu-panel");

  // Cerrar al hacer click fuera
  document.addEventListener("click", (e) => {
    // @ts-ignore
    if (
      navMenu &&
      hamburger &&
      !navMenu.contains(e.target) &&
      !hamburger.contains(e.target)
    ) {
      navMenu.classList.remove("active");
      hamburger.classList.remove("active");
      document.body.classList.remove("no-scroll");
      hamburger.setAttribute("aria-expanded", "false");
    }
  });

  if (hamburger && navMenu) {
    hamburger.addEventListener("click", () => {
      const isActive = hamburger.classList.contains("active");

      hamburger.classList.toggle("active");
      navMenu.classList.toggle("active");
      hamburger.setAttribute("aria-expanded", (!isActive).toString());

      document.body.style.overflow = isActive ? "" : "hidden";
    });

    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape" && navMenu.classList.contains("active")) {
        hamburger.classList.remove("active");
        navMenu.classList.remove("active");
        hamburger.setAttribute("aria-expanded", "false");
        document.body.style.overflow = "";
      }
    });

    // Cerrar al hacer click en los links
    const navLinks = navMenu.querySelectorAll(".menu-link");
    navLinks.forEach((link) => {
      link.addEventListener("click", () => {
        hamburger.classList.remove("active");
        navMenu.classList.remove("active");
        hamburger.setAttribute("aria-expanded", "false");
        document.body.style.overflow = "";
      });
    });
  }
</script>

<style>
  .logo-box {
    font-size: 2rem;
    font-weight: bold;
  }

  .logo {
    text-decoration: none;
    color: var(--color-bg);
    background-color: var(--color-primary);
    font-size: 2.1rem;
    padding: 0.25rem 0.75rem;
    border-radius: 0.5rem;
    transform: rotate(-3deg);
    display: inline-block;
  }

  /* --- HAMBURGER MENU REFACTORIZADO --- */
  .hamburger {
    display: none; /* Oculto en escritorio */

    /* Resets críticos para iOS */
    -webkit-appearance: none;
    appearance: none;
    background: transparent;
    border: none;
    -webkit-tap-highlight-color: transparent;

    cursor: pointer;
    padding: 0;
    width: 30px;
    height: 30px;
    position: relative;
    z-index: 1000;
  }

  .bar {
    display: block;
    height: 3px;
    width: 100%;
    border-radius: 3px;
    background: var(--color-text);
    transition: all 0.35s cubic-bezier(0.4, 0, 0.2, 1);
    transform-origin: center;
    /* Importante: Sin márgenes, usamos gap en el padre */
    margin: 0;
  }

  .hamburger.active .bar {
    background: var(--color-primary);
  }

  /* Animación X */
  /* Cálculo: Barra(3px) + Gap(6px) = 9px de desplazamiento */
  .hamburger.active .bar:nth-child(1) {
    transform: translateY(9px) rotate(45deg);
  }

  .hamburger.active .bar:nth-child(2) {
    opacity: 0;
  }

  .hamburger.active .bar:nth-child(3) {
    transform: translateY(-9px) rotate(-45deg);
  }

  /* --- NAVBAR STYLES --- */
  .navbar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    background-color: var(--color-bg);

    transition: all 0.3s ease;
    animation: fadeInDown linear forwards;
    animation-timeline: scroll();
    animation-range: 0px 10px;
  }

  @keyframes fadeInDown {
    from {
      box-shadow: 0 0 0 rgba(0, 0, 0, 0);
    }
    to {
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
      backdrop-filter: blur(10px);
    }
  }

  .navbar.scrolled {
    background-color: var(--color-bg);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
    backdrop-filter: blur(10px);
  }

  .nav-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-left: 1.3rem;
    padding-right: 1.3rem;
    width: 100%;
    max-width: 1200px;
    height: 100%;
    margin: 0.5rem auto;
    font-family: var(--font-title);
  }

  .menu-panel {
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .menu-links {
    display: flex;
    justify-content: space-between;
    align-items: center;
    list-style: none;
    margin: 0;
  }

  .menu-link {
    font-family: var(--font-title);
    font-size: 1.2rem;
    text-decoration: none;
    color: var(--color-text);
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    height: 2.4em;
  }

  .link-content {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0 1em;
    height: 100%;
  }

  .link-text {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    padding: 0.2em 0;
  }

  .link-text span {
    white-space: nowrap;
    text-transform: uppercase;
    transform: translateY(5%) rotate(0.001deg);
    transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  .link-text span.duplicate {
    position: absolute;
    transform: translateY(125%) rotate(0.001deg);
    color: var(--color-primary);
  }

  @media (hover: hover) {
    .link > .link-hover:hover .link-text span {
      transform: translateY(-115%) rotate(0.001deg);
    }
    .link > .link-hover:hover .link-text span.duplicate {
      transform: translateY(5%) rotate(0.001deg);
    }
  }

  @media (width < 1199px) {
    .navbar {
      width: 100%;
    }

    .nav-content {
      width: 100%;

      margin-top: 0.5rem;
      margin-bottom: 0.5rem;
    }
  }

  /* Mobile */
  @media (width < 600px) {
    .hamburger {
      /* Aquí activamos el Flexbox mágico */
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 6px; /* Espacio exacto entre barras */
    }

    .menu-panel {
      position: absolute;
      top: 65px;
      left: 0;
      transform: translateX(100%);
      width: 100vw;
      padding: 0.1rem 0;
      background-color: var(--color-bg);
      box-shadow: 0 16px 20px rgba(0, 0, 0, 0.15);
      backdrop-filter: blur(10px);
      border-bottom-left-radius: 1rem;
      border-bottom-right-radius: 1rem;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      overflow-y: auto;
      gap: 0;
      transition: transform 0.3s cubic-bezier(0.33, 1, 0.68, 1);
    }

    .menu-panel.active {
      transform: translateX(0);
    }

    .menu-links {
      flex-direction: column;
      width: 100%;
      gap: 0;
    }

    .menu-links li {
      width: 100%;
    }

    .menu-links li a {
      display: block;
      width: 100%;
      padding: 1rem 0;
      font-size: 1.5rem;
      text-align: center;
    }
  }
</style>
